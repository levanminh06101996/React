const config = {
  routes: {
    home: "/",

    // Auth
    login: "/login",
    login2: "/login2",

    // Products
    products: "/products",
    productDetail: "/products/:slug",

    // Users
    users: "/users",

    notFound: "*",
  },
};

export default config;
